{
  "name": "web-1",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "panda codegen",
    "dev": "next dev -p 4001",
    "build": "next build",
    "start": "next start",
    "lint": "biome lint ./src",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf .next && rm -rf dist && rm -rf styled-system"
  },
  "dependencies": {
    "@pandacss/dev": "^0.33.0",
    "@three/const": "*",
    "@three/libs": "*",
    "@three/ui": "*",
    "next": "14.1.2",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18"
  }
}
