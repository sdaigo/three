{
  "name": "@three/foundation",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "prepare": "panda codegen",
    "dev": "panda studio",
    "build": "tsup ./src/index.ts",
    "lint": "biome lint ./src",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist && rm -rf styled-system"
  },
  "dependencies": {},
  "devDependencies": {
    "@pandacss/dev": "^0.33.0",
    "@pandacss/studio": "^0.33.0",
    "tsup": "^8.0.2"
  }
}
